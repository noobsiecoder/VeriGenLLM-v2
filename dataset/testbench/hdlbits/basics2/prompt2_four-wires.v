// Create a module with these wire connections:
// - Input 'a' connects to output 'w'
// - Input 'b' connects to outputs 'x' and 'y' (used twice)
// - Input 'c' connects to output 'z'
module four_wires ( 
    input a, b, c,
    output w, x, y, z 
);
// Use assign statements
// Insert code here